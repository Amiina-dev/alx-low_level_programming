#include "main.h"

/**
 * _memcpy - copies memry area
 * @dest: destination
 * @src: source
 * @n: size
 * Return: dest
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
	{
		AV
	}
	return (dest);
}
